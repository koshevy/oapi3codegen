import * as _ from 'lodash';

export class CliApplication {

    /**
     * Start point of lib application
     */
    public run(): void {

    }

    protected processApiService(options: {
        apiSchemaFile,
        baseTypeName,
        method,
        paramsModelName,
        paramsSchema,
        path,
        queryParams,
        responseModelName,
        responseSchema,
        requestModelName,
        requestSchema,
        servers,
        typingsDependencies: string[],
        typingsDirectory: string,
    }): void {

    }
}